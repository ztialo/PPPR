
            # if offset != None:
            #     threshold = 20
            #     if abs(offset) < threshold:
            #         robot.forward(speed=30, force=5)
            #     elif offset > threshold:
            #         robot.turn_right(speed=20, force=5)
            #     else:
            #         robot.tur